/*
COP3223 Fall 2023 Assignment 1_1
Copyright 2023 Grieves_Elijah
*/
#include <stdio.h>
int main(void)
{
    printf("This program will allow you to enter\n"                                             //Informs the user of the program
            "employees' hours worked & their hourly rate and"
            "\nwill return their salary, accounting for overtime\n");
    
    double hoursWorked, hourlyRate, salary;                                                     //Initializes variables (All variables have the potential to not be integers)
                                                                                                //It is possible to work fractions of an hour. This is why hoursWorked is a double
    do {

        printf("\nEnter # of hours worked (-1 to end): ");                                      //Promptss for hours worked
        scanf("%lf", &hoursWorked);

        if (hoursWorked == -1)                                                                  //Ends the loop if the user enter "-1". I thought using a do...while loop 
            break;                                                                              //would circumvent the issue, but a while loop may have been better

        printf("Enter hourly rate of the worker ($00.00): ");                                   //Prompts for hourly rate
        scanf("%lf", &hourlyRate);

        if (hoursWorked > 40.0) {
            salary = (hourlyRate * 40.0) + ((hoursWorked - 40.0) * (hourlyRate * 1.5));         //Calculates salary if there is overtime by multiplying the difference
        }                                                                                       //between hoursWorked and 40 by the time-and-a-half hourlyRate
        else {
            salary = hourlyRate * hoursWorked;                                                  //Calculates salary if no overtime
        }

        printf("Salary is $%.2lf\n", salary);                                                   //Displays the value of salary with two point precision

    }   while (hoursWorked != -1);

    return 0;
}
